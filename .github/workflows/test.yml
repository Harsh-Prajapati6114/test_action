name: Sync S3 Bucket  

on:
  push:
    branches:
      - stage 
jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: print hello 
        run: echo "Hello World successfully triggered on stage branch commit"
